/// <reference types="react" />
import { AppJob } from '../../../../@types/app';
import { Status } from '../../constants';
export declare const Timeline: ({ job, status, }: {
    job: AppJob;
    status: Status;
}) => JSX.Element;
